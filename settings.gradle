pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'one-for-logis'

include 'common-lib', 'user-service', 'hub-service', 'order-service', 'gateway-service', 'eureka-server', 'company-service', 'notification-service'
